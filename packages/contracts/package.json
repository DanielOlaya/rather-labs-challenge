{
  "name": "contracts",
  "private": true,
  "version": "0.0.1",
  "description": "Smart contract ABIs and types for cross-chain lending protocol",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "generate-types": "typechain --target ethers-v6 --out-dir src/types './src/abis/*.json'"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "typechain": "^8.3.0",
    "@typechain/ethers-v6": "^0.5.0"
  }
}